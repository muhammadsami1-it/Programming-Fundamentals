#include<stdio.h>
int main (void)
{
	int testInteger=30000000000;
	printf("Number is %d",testInteger);
	return 0;
}
//the reason that the output is wrong because it far exceeds the value that an integer "d"datatype can hold